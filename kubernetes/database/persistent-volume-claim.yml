apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-config
  labels:
    type: local
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
  volumeName: postgres-pv
  storageClassName: standard
